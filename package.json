{
  "name": "beast-tsrx",
  "version": "0.2.0",
  "description": "An indentation-based frontend that compiles .btsx components to Octane-compatible TSRX.",
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./vite": {
      "types": "./dist/vite.d.ts",
      "default": "./dist/vite.js"
    }
  },
  "bin": {
    "beast": "dist/cli.js"
  },
  "files": [
    "dist",
    "docs",
    "examples",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p packages/create-beast/tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p packages/create-beast/tsconfig.json --noEmit",
    "test": "bun test",
    "check": "bun run typecheck && bun run test && bun run build",
    "prepack": "bun run build",
    "pack:check": "npm pack --dry-run && npm pack --dry-run --workspace create-beast"
  },
  "keywords": [
    "btsx",
    "tsrx",
    "octane",
    "compiler",
    "templates"
  ],
  "author": "",
  "license": "ISC",
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=22.22.2"
  },
  "peerDependencies": {
    "octane": "0.1.37",
    "vite": "^8.0.16"
  },
  "peerDependenciesMeta": {
    "octane": {
      "optional": true
    },
    "vite": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^26.2.0",
    "happy-dom": "^20.11.0",
    "octane": "^0.1.37",
    "typescript": "^5.9.3",
    "vite": "^8.0.16"
  }
}
